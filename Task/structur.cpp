#include <stdio.h>
struct 
{
    int no;
    char name[50];
}to;

int main()
{
    to.no;
    to.name;
        printf("enter the number of person  =  ");
        scanf("%d",&to.no);
        printf("enter the name  =  ");
        scanf("%s",&to.name);
        printf("no of person =  %d\n",to.no);
        printf("person name =  %s",to.name);
    
}

